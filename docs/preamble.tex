% preamble.tex
\usepackage[margin=1in]{geometry}
\usepackage{setspace}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{pdflscape}
\usepackage{graphicx}
\usepackage[outputdir=build]{minted}
\usepackage{fontspec}
\usepackage{tcolorbox}

% Fonts
\setmonofont[Scale=0.8]{JetBrains Mono}

% Colors
\definecolor{background_color}{RGB}{27,25,25}
\definecolor{codebg}{RGB}{47,45,45}
\pagecolor{background_color}
\color{white}

% Minted
\usemintedstyle{monokai}
\setminted{
    bgcolor=codebg,
    breaklines,
    baselinestretch=1.2
}

% Inline code boxes
\newtcbox{\inlinecode}[1][codebg]{%
    on line,
    arc=2.5pt,
    colback=#1,
    colframe=#1,
    boxrule=0pt,
    boxsep=0pt,
    left=3pt, right=3pt, top=3pt, bottom=3pt,
    before upper={\vphantom{Ag}},
    fontupper=\ttfamily\color{white}
}

% Line spacing
\setstretch{1.25}

% Paragraph formatting
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.8em}

% Section numbering
\renewcommand{\thesection}{HW \#\arabic{section}}
\renewcommand{\thesubsection}{\arabic{section}.\arabic{subsection}}

% Section style
\titleformat{\section}
  {\normalfont\Large\bfseries}
  {\thesection}
  {1em}
  {}

% Start each section on new page and fix TOC
\let\oldsection\section
\renewcommand{\section}[1]{%
    \clearpage%
    \addtocontents{toc}{\protect\setcounter{tocdepth}{2}}%
    \refstepcounter{section}%
    \addcontentsline{toc}{section}{\arabic{section}\quad #1}%
    \oldsection*{HW \#\arabic{section}\quad #1}%
}